import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  box: {
    backgroundColor: 'white',
    width: '100%',
    height: '100%',
    alignSelf: 'center',
    paddingTop: 0,
    marginTop: 0,
  },
});
