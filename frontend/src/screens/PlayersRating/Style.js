import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  rateButton: {
    borderColor: '#FFFFFF',
    borderWidth: 1,
  },
  rateText: {
    color: '#FFFFFF',
  },
  ratePressed: {
    backgroundColor: 'gray.700',
  },
});
